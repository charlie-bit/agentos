# The single convergence point: four name references, zero inline config.
# Switching business = switching this file. "entry: cli" resolves against the
# names exported by @agentos/entry-cli (code registry, per contract).
kind: preset
version: "1"
name: example
knowledge: local-markdown
model: deepseek
tools:
  - mcp-filesystem
  - mcp-remote-example
entry: cli
