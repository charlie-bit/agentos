# The single convergence point: four name references, zero inline config.
# Switching business = switching this file. "entry: console" resolves against
# the cli's registered adapters (P2 will replace this placeholder registry).
kind: preset
version: "1"
name: example
knowledge: local-markdown
model: deepseek
tools:
  - mcp-filesystem
  - mcp-remote-example
entry: console
